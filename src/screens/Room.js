import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Room extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Room </Text>
      </View>
    );
  }
}

export default Room;
